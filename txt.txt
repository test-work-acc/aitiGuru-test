src/
├── api/                    # API-слой
│   ├── client.ts          # axios/fetch инстанс с интерцепторами
│   ├── auth.ts            # login, refresh, logout
│   └── products.ts        # getAll, search, add (mock)
├── components/             # Переиспользуемые компоненты
│   ├── ui/                # Button, Input, Modal, Toast (атомарные)
│   ├── products/          # ProductTable, ProductRow, SortHeader
│   └── auth/              # LoginForm, AuthLayout
├── hooks/                  # Кастомные хуки
│   ├── useAuth.ts         # логика сессии + хранение токена
│   ├── useProducts.ts     # обёртка над React Query
│   └── useSort.ts         # логика сортировки (если не через API)
├── store/                  # State management
│   ├── authStore.ts       # Zustand: token, user, rememberMe
│   └── uiStore.ts         # модалки, тосты, лоадеры
├── pages/                  # Страницы маршрутов
│   ├── LoginPage.tsx
│   └── ProductsPage.tsx
├── types/                  # TypeScript интерфейсы
│   ├── api.d.ts           # DTO для ответов API
│   └── domain.d.ts        # бизнес-сущности (Product, User)
├── utils/                  # Хелперы
│   ├── format.ts          # форматирование цены, рейтинга
│   └── storage.ts         # абстракция над localStorage/sessionStorage
├── App.tsx
├── main.tsx
└── routes.tsx             # React Router v6